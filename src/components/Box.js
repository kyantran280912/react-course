import React from "react";

const Box = () => {
    return (
        <div
            style={{
                width: "100px",
                height: "100px",
                backgroundColor: "purple",
                borderRadius: "10px",
            }}
        ></div>
    );
};

export default Box;
